# Playwright Automation Rules

- Do NOT create new test files
- Modify only existing test files
- Reuse existing Page Object methods
- If a method is missing, add it to the Page Object
- Do NOT use page.locator in test files
- Tests must call business-level methods only
- Use frameLocator() for iframes
